# Core classifier + cleaner use only the Python standard library.
# These are needed ONLY for the live Gmail commands (classify / clean):
google-api-python-client>=2.120.0
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
